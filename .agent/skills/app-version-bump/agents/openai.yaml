interface:
  display_name: "App Version Bump"
  short_description: "Refresh version after changes"
  default_prompt: "Use $app-version-bump to update Food-list manifest.json version so the service worker refreshes cached menu data and pages."
